import React from 'react'
import ReactDOM from 'react-dom/client'
import FleetCommand from './FleetCommand.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FleetCommand />
  </React.StrictMode>
)
